language: node_js
node_js:
  - "8.2.1"

install:
  - npm install -g newman

before_script:
  - node --version
  - npm --version
  - node_modules/.bin/newman -v

script:
  - node_modules/.bin/newman run collections/Postman Echo.postman_collection.json
