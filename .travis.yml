language: node_js
node_js:
  - "8.2.1"

install:
  - npm install -g newman

before_script:
  - node --version
  - npm --version
  - sudo ln -s /usr/bin/nodejs /usr/bin/node
  - which newman
  - newman -v

script:
  - newman run collections/Postman Echo.postman_collection.json
